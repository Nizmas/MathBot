

namespace MathBot.Dal.Implementations.Extensions;

public static class MathBotDalExtension
{
    /*public static IServiceCollection RegistrationTelegramDal(this IServiceCollection services, IConfiguration config)
    {
        services.TryAddScoped<IMessageLogProvider, MessageLogProvider>();
        services.RegistrationNpgsql<ITelegramServiceDbContext, TelegramServiceDbContext>(config);
        
        return services;
    }*/
    
}